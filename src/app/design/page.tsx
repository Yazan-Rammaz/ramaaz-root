import { DesignGallery } from './DesignGallery';

/**
 * /design — the gallery index. The layout above already 404s this outside
 * development, so there is nothing to check here.
 */
export default function DesignPage() {
    return <DesignGallery />;
}
