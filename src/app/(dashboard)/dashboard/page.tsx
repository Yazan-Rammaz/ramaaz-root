/**
 * Dashboard — blank canvas. Build the real dashboard from XD here, following
 * the conventions in AGENTS.md (XD-pixel utilities, i18n keys, UI primitives).
 *
 * Auth is handled one level up: the protected (dashboard) layout runs the
 * `requireSession()` gate. Add `const user = await requireSession()` here if you
 * need the current user on this page.
 */
export default function DashboardPage() {
  return (
    <div className="flex flex-col gap-20">
      {/* TODO: build the dashboard from XD */}
    </div>
  );
}
